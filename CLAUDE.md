# CLAUDE.md

このファイルは、このリポジトリ(`server-manager-client`)でAIエージェント(Claude/Cursor等)が作業する際に従うべきルールを定義する。

## バージョンタグ・リリースに関するルール

このアプリ(Electronクライアント)は、GitHub Releasesを使った`electron-updater`による自動更新の仕組みを持つ。**`vMAJOR.MINOR.PATCH`形式のタグをpushすると、GitHub Actionsが自動的にビルド・GitHub Releaseの作成・ユーザーへの自動配信を行う。** そのため、バージョンタグのpushは「アプリの最新バージョンとして全ユーザーに配信する」ことを意味する、影響範囲の大きい操作である。

### 通常のプッシュ(タグなし)

開発中の変更は、これまで通りタグを付けずに通常のブランチ/コミットとしてpushしてよい。この場合、GitHub Releaseは作成されず、既存ユーザーへの自動更新も発生しない。

- **`git push`(タグなし)は、最終リリースの確認を挟まず、通常の作業として実行してよい。**

### 最終リリースのプッシュ(タグあり)

以下の場合のみ、バージョンタグ付きのプッシュを行う:

1. `package.json`の`version`を`MAJOR.MINOR.PATCH`(セマンティックバージョニング)形式で更新する。
2. `vMAJOR.MINOR.PATCH`(例: `v0.2.0`)の形式でgitタグを作成する。
3. タグをpushする(例: `git push origin vMAJOR.MINOR.PATCH`)。

**タグ付きプッシュを行う前に、AIエージェントは必ずユーザーに対して「これは最終リリースとして公開してよいバージョンか」を明示的に確認し、承認を得てから実行すること。** ユーザーから明確な承認(バージョン番号の確認を含む)が得られるまでは、タグの作成・pushを行ってはならない。

理由: タグpushは`.github/workflows/release.yml`をトリガーし、ビルド成果物をGitHub Releaseとして公開し、既存ユーザーの`electron-updater`に自動更新として配信される。誤ったタイミング・バージョンでのタグpushは、意図しないバージョンを全ユーザーに配信してしまうリスクがある。

### まとめ

| 操作 | タグ | 確認 | 影響 |
|---|---|---|---|
| 通常の開発プッシュ | なし | 不要 | リリースは作成されない |
| 最終リリースプッシュ | `vMAJOR.MINOR.PATCH` | **必須(ユーザーへの事前確認)** | GitHub Releaseが作成され、全ユーザーに自動更新配信される |
